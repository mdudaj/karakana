# P06: catalogue review and PR integration

Status: author review and local checks complete; ready for PR review, 2026-09-26.
User authorized P06 review and PR integration
after the concrete P05 delivery. Branch: skills/engineering-documentation-catalogue.
Parent eee065a; upstream base 5e4cb9f (fetched and checked).

## Scope, evidence and acceptance

Review the complete P01–P05 change against the original global/generic requirement,
Markdown primary/audience Excel correction, established lifecycle and source/feedback
safety contract. Reuse [research](RESEARCH.md), [plan](PLAN.md), [P01](P01.md),
[P02](P02.md), [P03](P03.md), [P04](P04.md), [P05](P05.md),
[contract](../../engineering-artifacts.md), [ADR 0005](../../adr/0005-markdown-engineering-artifact-contract.md)
and [pilot QA](pilots/QA.md). Inspect actual skill bodies/references, module, schemas,
tests/evals, pack deltas, generated assets and upstream diff. Apply github-pr-review
and karakana-self-improvement; review correctness/security/spec, not style-only issues.

| ID | Acceptance | Evidence |
| --- | --- | --- |
| A01 | Global blank assets remain generic; four authoring and one export skill are experimental, optional and request-scoped | Full source/spec diff and validators |
| A02 | Native authority/identity, read-only import, immutable baselines and explicit feedback remain intact | Module/schema review and focused regression |
| A03 | Reproduced correctness/safety findings resolved before publication | Sparse-input/comment fixtures, relevant tests and scoped transport limits |
| A04 | Research/source/share-pack parity and required full gate pass on the reviewed revision | Isolated pytest/evals, skill/pack/memory checks and actual asset checks |
| A05 | Concrete PR includes scope, verification, risk and actual readiness; protected-branch integration uses PR/squash | Verified remote base/head, PR/CI/review records and append-only handoff |

As a workbook reviewer, I want sparse/oversized input refused before rectangular
iteration and unbound comments reported, so review cannot exhaust memory or hide
annotations. The product need and scope are the existing P01/P02 read-only feedback
contract, not a new feature family. F01 maps to plan REQ11/REQ12/REQ18/REQ19 and A02–A04;
F02 maps to REQ10/REQ12/REQ19 and A02–A04. New test names and final outcomes below
provide change-to-verification traceability. No migration or new ADR is applicable.

Definition of Done for the fixes: reproduced cases, bounded import/export scan,
visible preserved blank-row comments, no input mutation, focused and full required
checks pass, review/usage limits updated and task-bound handoff. Artifact readiness:
current P01/P02 contract/ADR, P05 source/transport evidence, this scope/acceptance/test
rationale and isolated baseline are inspected before editing. The additional
python-code-change trace binds F01/F02 implementation; umbrella skill trace binds
the catalogue/PR delivery. Presence checks do not substitute for these actual results.

User acceptance covers local fixes and the proposed P06 PR workflow; no personal
install, live model/provider execution, deployment or real release drafting follows.
Do not claim independent review from this continuing author. Apply any actual remote
review/required-check gates, and distinguish PR preparation/publication/merge.
Human review remains required under the existing self-improvement skill; user slice
approval is not invented GitHub review evidence. Existing external-action authority
must govern integration. No automatic merge or gate bypass.

## Findings and implementation instructions

F01 (P1, resolved), resource-bound gap: compressed/expanded ZIP limits do not bound rectangular
worksheet iteration. A tiny returned workbook with a distant row can induce millions
of cell visits/allocations. Inspect `_read_workbook`, `_sheet_records` and export
projection; add a shared total scan-cell limit before feedback iteration, and reject
oversized projections before building/writing a workbook. Cover sparse row/column
inputs and output refusal; preserve all source/baseline/returned bytes. Document the
same limit in contract/tool usage. This extends existing plain-workbook safety, not
an architecture/approval change; existing ADR/source contract govern.

F02 (P2, resolved), annotation coverage: inspect comments on otherwise blank rows. Preserve/report
the comment even without a record identity; do not silently treat it as a source edit
or drop it. Add a real returned workbook regression if reproduction confirms the gap.

Before implementation attach this requirements/test rationale and run prerequisite
protocol check. Run baseline deterministic evals in an isolated copy. Reproduce with
temporary synthetic files; do not actually execute an unbounded scan. Make focused
patches, run the new/affected tests, then isolated full suite/evals/validators. Check
source and workbook parity and all generic/experimental/project boundaries. Record
semantic limitations and source/license provenance honestly. Safely archive/check the
unannotated research pack before updating continuation. Prepare exact PR title/body,
publish the reviewed task branch when authorized, and verify actual CI/review state.

## Review outcomes and verification

The safe F01 reproduction saved a 14,513-byte XLSX with a cell at row 1,000,000,
implying 18,000,000 rectangular cells. The old reader accepted it; an unbounded
feedback scan was deliberately not run. Import now rejects more than 500,000 total
rectangular cells before header/row iteration; export rejects the same projected
range before constructing/writing a pair. F02 reproduction attached a comment to
an empty Feedback cell and observed zero comments. Comments are now collected
before empty/invalid-identity row filtering; empty identity stays unbound.

| Check | Actual result |
| --- | --- |
| New regressions before fixes | Five failures reproduced: sparse row/column/aggregate range, export refusal and empty-row comment |
| New regressions after fixes | All five pass; input bytes preserved and refused outputs absent |
| Focused tool suite | 52 passed |
| Isolated full suite | 681 passed in 24.48 seconds; Python 3.14.4, openpyxl 3.1.5 |
| Baseline/final deterministic evals | 171 passed in both; no failures/warnings; instruction coverage, not live model behavior |
| Catalogue and protocols | 43 skills, 9 skillpacks and 10 protocols valid; Karakana/LIMS/ent-meal memories complete |
| Skillpack warnings | Existing absent billing, msc-research and nhrdm memory directories in isolated copy; no target-project warning |
| Assets | Research 0.6 archived only after exact source/JSON/XLSX parity and zero annotations; new 0.7 has 15 sheets/226 rows, exact source hash and literal text |
| Blank/pilot preservation | Generic template Markdown/XLSX byte-identical to eee065a; six original pilot pairs still pass committed hashes/parity and zero-feedback tests |
| Upstream scope | Base 5e4cb9f fetched; only optional additions in three packs, global pointers, docs/assets, five skill directories, opt-in module/dependency and tests/evals |

Reviewed all five entrypoints and conditional guides against the contract and
request boundary. Native field/ID authority, read-only import/binding proposals,
immutable baselines, stale/conflicting three-way feedback and audience blocker
retention remain covered. No routing, protected workflow, safety policy or native
engine schema changed. Source/license register is original authorship with research
attribution, not copied third-party skill implementation or a new distribution grant.
No remaining P0/P1 finding in this author review; it is not independent approval.

Logs, parity receipts and isolated runtime are recorded under
`.karakana/research/engineering-artifacts-20260926/p06/` (not committed). Final
eval receipt: `20260926-152420-eval-7e095e`. The PR description and bound handoff
record actual publication/head/CI state after the branch is published; no successful
merge, native Excel check or substantive recipient review is predeclared here.

## Risks, recovery and continuation

Medium local data-integrity/resource risk. The tool is opt-in and performs no network,
source apply, native regeneration or operational action. Review evidence fields for
structural completeness; do not claim authentic approvals or observed product truth
from parser success. Macro/external-link/formula/unsupported-feature limits apply.
Revert through a reviewed PR if rejected; preserve canonical sources and annotations.

Known limits remain native Excel/assistive/recipient usability, independent substantive
author review, real project application and stable promotion. P05 office/source checks
are scoped local evidence. Next exact action depends on actual PR state: inspect the
concrete diff/checks/reviews and integrate by squash only when its gates are satisfied;
then use the experimental skills for a separately requested project documentation task.
