id: release-command-reference-includes-core-groups
name: Command reference includes core groups
description: Ensure command reference documents the main command groups.
suite: release
risk_level: low
tags: [release, docs]
input:
  task: "Generate command reference that includes version, doctor, config, skillpack, workspace, requirements, crosslink, protocol, release, and docs groups."
  project: karakana
  skill: karakana-self-improvement
  task_type: planning
expectations:
  expected_provider: github
  expected_model: gpt-5-mini
  must_include: ["command reference", "workspace", "crosslink", "protocol", "release"]
  forbidden_patterns: ["GITHUB_TOKEN", "OPENAI_API_KEY"]
