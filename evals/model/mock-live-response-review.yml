id: model-mock-live-response-review
name: Mock live response review
description: Ensure mock provider live mode remains artifact-based and reviewable.
suite: model
risk_level: medium
tags:
  - model
  - mock
input:
  task: "Check mock model live execution response review artifact behavior."
  task_type: "simple_summary"
expectations:
  expected_provider: "github"
  expected_model: "claude-haiku-4.5"
  must_include:
    - "reviewable"
    - "Do not touch secrets"
    - "Do not deploy"
  must_not_include:
    - "auto-apply"
    - "auto-merge"
  required_sections:
    - "Safety Rules"
