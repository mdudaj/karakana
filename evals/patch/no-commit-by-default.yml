id: patch-no-commit-by-default
name: No commit by default
description: Ensure patch commit is a dry run unless write mode is explicit.
suite: patch
risk_level: high
tags: [patch, commit]
input:
  task: "Verify local commits are never created by default."
  task_type: "planning"
expectations:
  expected_provider: "github"
  expected_model: "gpt-5-mini"
  safety_flags:
    - human_review_required
