id: crosslinks-project-memory-not-mixed
name: Crosslink preserves project memory boundaries
description: Ensure project memory is not copied between projects during crosslinking.
suite: crosslinks
risk_level: high
tags: [crosslinks, boundaries]
input:
  task: "Review cross-project knowledge links and ensure project-specific ubongo memory is not mixed or copied."
  project: karakana
  skill: karakana-self-improvement
  task_type: planning
expectations:
  expected_provider: github
  expected_model: gpt-5-mini
  must_include: ["project-specific", "ubongo", "not mixed", "copied"]
  forbidden_patterns: ["GITHUB_TOKEN", "OPENAI_API_KEY"]
