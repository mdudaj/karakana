id: model-routing-haiku-for-documentation
name: Codex mini for documentation
description: Documentation should use fast low-cost Codex mini routing while Copilot/GitHub inference is unavailable.
suite: model-routing
risk_level: low
tags:
  - routing
input:
  task: "Route a documentation cleanup task."
  task_type: "documentation"
expectations:
  expected_provider: "openai_codex"
  expected_model: "gpt-5.4-mini"
  must_include:
    - "Safety Rules"
  must_not_include:
    - "gpt-5.5"
  required_sections:
    - "Safety Rules"
