id: model-routing-copilot-max-planner-budget
name: Codex mini planner budget
description: Planning and requirements work should stay on Codex mini with a standard planner budget while Copilot/GitHub inference is unavailable.
suite: model-routing
risk_level: medium
tags:
  - routing
  - copilot-max
  - token-efficiency
input:
  task: "Plan a feature and identify requirements before implementation."
  task_type: "planning"
expectations:
  expected_provider: "openai_codex"
  expected_model: "gpt-5.4-mini"
  must_include:
    - "planner"
    - "standard"
  required_sections:
    - "Token Policy"
