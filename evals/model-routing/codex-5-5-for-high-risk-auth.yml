id: model-routing-codex-5-5-for-high-risk-auth
name: Codex GPT-5.5 for high-risk auth
description: Authentication and authorization work should route to GPT-5.5.
suite: model-routing
risk_level: high
tags:
  - routing
  - security
input:
  task: "Route an OAuth and permission change."
  task_type: "security_or_auth_change"
expectations:
  expected_provider: "openai_codex"
  expected_model: "gpt-5.5"
  must_include:
    - "Safety Rules"
  required_sections:
    - "Safety Rules"
