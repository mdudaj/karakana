id: actions-suggested-skills-extracted
name: Suggested skills extracted
description: Ensure action bundles can preserve suggested skill context.
suite: actions
risk_level: low
tags:
  - actions
  - skills
input:
  task: "Check suggested skill extraction for Viewflow, Invenio, GePG, Django, PR review, handoff, and write-skill workflows."
  task_type: "simple_summary"
expectations:
  expected_provider: "github"
  expected_model: "claude-haiku-4.5"
  must_include:
    - "reviewable"
    - "approval"
    - "tests"
  required_sections:
    - "Safety Rules"
  forbidden_patterns:
    - "GITHUB_TOKEN"
    - "OPENAI_API_KEY"
