id: codex-review-patch-escalates-viewflow-state-risk
name: Review patch escalates Viewflow state risk
description: Ensure Viewflow process-state risk uses GPT-5.5 routing.
suite: codex
risk_level: high
tags: [codex, viewflow]
input:
  task: "Review a Viewflow process-state transition patch."
  task_type: "viewflow_process_state_change"
expectations:
  expected_provider: "openai_codex"
  expected_model: "gpt-5.5"
  must_include:
    - "Safety Rules"
  required_sections:
    - "Safety Rules"
