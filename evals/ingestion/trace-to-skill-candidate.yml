id: ingestion-trace-to-skill-candidate
name: Trace ingestion proposes skill candidate
description: Ensure trace lessons are distilled into skill update candidates for review.
suite: ingestion
risk_level: medium
tags: [ingestion, skills, traces]
input:
  task: "Classify a trace containing repeated workflow and verification step evidence into a skill update candidate."
  project: karakana
  skill: write-karakana-skill
  task_type: planning
expectations:
  expected_provider: github
  expected_model: gpt-5-mini
  must_include: ["repeated workflow", "verification step", "skill update candidate"]
  forbidden_patterns: ["OPENAI_API_KEY", "GITHUB_TOKEN"]
